# 需求变更对照表（业务架构师评审意见回应）

| 项目 | 内容 |
|------|------|
| 模块 | 工卡管理 Task Card Management（LGS-TS-01-01） |
| 依据蓝图 | xxxTS蓝图报告-20260319.md 第 2.3.4 节、2.1.1 节 TS-01 |
| 评审意见来源 | 客户业务架构师审核结论（8 项实质遗漏 / 5 项理解偏差 / 6 项待澄清） |
| 变更前 | 需求 1–32，正确性属性 17 条 |
| 变更后 | 需求 1–43，正确性属性 24 条 |
| 受影响文档 | requirements.md、design.md、tasks.md |
| 日期 | 2026-08-09 |
| 状态 | 待业务方签核 |

---

## 一、必须补充的遗漏（P1）— 8 项，全部已补

| 序 | 评审指出的缺口 | 蓝图依据 | 处理方式 | 新增/修改条目 | 数据模型影响 | 签核 |
|----|---------------|---------|---------|--------------|-------------|------|
| 1 | 工卡起止时间字段缺失 | 用户需求 I.10 "the start time and finish time of the work card" | 新增需求，工卡与工序**两级**记录开始/结束时间；首道工序开始→工卡开始，全部完成→工卡结束；均为只读，由执行期报工写入 | **需求 33**（9 条 AC）<br>Property 18 | `task_card.start_time`/`finish_time`<br>`process_step.start_time`/`finish_time`<br>`job_process.start_time`/`finish_time` | ☐ |
| 2 | 审核流程与状态机不完整 | 工作分解步骤 60「编制完成的工卡进行审核」；一编一审局方合规 | 状态由三值扩为**四值**（新增/**审核中**/生效/作废）；新增提交审核、批准、驳回流转与触发条件；审核意见必填并按版本归档；审核中禁止编辑；升版不继承审核结果 | **需求 34**（10 条 AC）<br>需求 4.4/4.5/4.6 改写<br>Property 1 重写、Property 19 | `CARD_STATUS` 增 `UnderReview`<br>新增 `ALLOWED_TRANSITIONS`<br>新增表 `review_record`（comment NOT NULL） | ☐ |
| 3 | 组织名称（I.1）无字段级需求 | 用户需求 I.1；蓝图「可在系统配置或工卡抬头中体现」 | 明确来源为系统参数配置，呈现于工卡抬头与打印，工卡上只读 | **需求 35**（3 条 AC） | 新增表 `system_parameter` | ☐ |
| 4 | 是否 FAI、模板类型只展示无来源 | 蓝图清单界面「是否FAI、模板类型」 | 在编制界面提供维护能力；FAI 默认「否」；清单展示引用该字段 | **需求 36**（4 条 AC） | `task_card.is_fai`/`template_type`（已存在，补需求来源） | ☐ |
| 5 | Work Scope 缺边界声明 | 2.3.4「工程部门核心操作功能（3项）」；工作分解步骤 30 | Introduction 重写为四段式边界声明，明确 Work Scope 为独立功能、仅经需求 25.3 对接 | Introduction「模块边界声明」 | 无 | ☐ |
| 6 | JOB NO 与执行期结构无对接声明 | 「未来工卡在执行过程中的结构」；2.3.6「JOB No 工程释放后生成」 | 声明 JOB No 生成触发点（工程释放）、与 Task Card/PID 的承载关系、多次释放各自独立、编制域与执行域互不修改 | **需求 37**（5 条 AC） | 新增表 `job`（job_no UNIQUE） | ☐ |
| 7 | 复制/升版后编号与初始状态规则缺失 | 与需求 10.5 查重规则直接冲突 | 复制须指定新编号并查重、状态置新增、版本初始；升版保持编号、版本+1、状态置新增；唯一性约束改为 (task_no, revision)；同编号最多一个生效版本 | **需求 38**（7 条 AC）<br>Property 20 | `UNIQUE(task_no, revision)` 语义确认<br>查重范围限定同版本 | ☐ |
| 8 | QA 能力清单依赖未声明 | 工作分解步骤 10「QA 维护能力主数据」 | 提交审核时校验（机型, 起落架类型, Skill）组合是否在有效能力清单内，超范围拒绝提交 | **需求 39**（4 条 AC）<br>Property 21 | 新增表 `capability_list` | ☐ |

---

## 二、理解偏差修正（P1–P2）— 5 项，全部已改

| 序 | 评审指出的偏差 | 我方原表述（错误） | 修正后表述 | 修改条目 | 我方确认 | 签核 |
|----|---------------|------------------|-----------|---------|---------|------|
| 1 | 需求 16.4 枚举值错误 | 「取值为『签署』或『签署-报工』」 | 除 SC 外，CR/PC/LT/BH/TS/NR/PR/EN/SW/TA 签署要求属性**均为「签署」**；SC 保留「单据不签署、所发工卡步骤需签署」特殊规则 | 需求 16.4 改写<br>需求 16.5 保留 | **接受**。确系把表头「签署or报工」当成了值域，属审核疏漏 | ☐ |
| 2 | 条码生成时机错位 | 「工序被创建时生成条码」（编制态） | 改为**工卡释放生成 JOB 时**按 `JOB No + Process ID` 生成；明确编制态不生成条码；条码须携带 JOB 上下文 | 需求 15 全部重写（4 条 AC）<br>Property 11 重写 | **接受**。原设计会导致同一工卡多次维修复用同一条码 | ☐ |
| 3 | 商务分类误设为人工选择项 | 「TS 工程师手选，超范围拒绝」 | 改为**按来源自动派生**：列明 7 类判定依据（工卡类型映射、工包划分、NRC 单据、零件性质、外包清单、计划设置）；记录派生依据来源；无法确定时方允许人工指定并标记 | 需求 29 全部重写（6 条 AC）<br>Property 23 | **接受**。蓝图「说明（CMC确认内容）」列确为派生规则 | ☐ |
| 4 | WBS 与工卡类型 01–11 关系未对齐 | 两处各自独立定义，未说明关系 | 明确为**同一字段的两种称法**，系统内单一字段存储，值域即 01–11；需求 8 的 IR 卡判断以该字段 = 04 为准 | 需求 6.8 改写 | **接受** | ☐ |
| 5 | 需求 12.2 采集项被收窄 | 「包含 P/N 与 S/N」（易被实现为固定两项） | 改为**可配置任意数量**的采集项，类型与需求 13.1 插入组件体系共用定义；P/N、S/N 降为常用类型示例 | 需求 12.2 改写 | **接受** | ☐ |

---

## 三、待澄清项处理（P2）— 6 项

| 序 | 澄清事项 | 处理方式 | 条目 | 待贵方动作 | 签核 |
|----|---------|---------|------|-----------|------|
| 1 | Ctrl Code 值域存疑 | 需求 6.7 与 design.md 枚举处**均已加「⚠ 待业务方确认」标记**，注明描述列错位（AS/FAB/OHV 等）及 AS/CL 与 Skill 代码重复 | 需求 6.7 批注 | **贵方回填正式值域**后我方复核 | ☐ |
| 2 | 打印模板差异（11 类工卡 + 11 类单据格式不同） | 新增按类型配置多套模板；无专属模板回退默认；所有模板均须隐藏工卡分类并完整呈现最小信息集 | **需求 40**（5 条 AC）<br>Property 22 | 提供各类现行纸质格式样张 | ☐ |
| 3 | PPC 工时只读性未声明 | 需求 11.3/11.4 显式声明**只读、TS 侧不可编辑**（工时由 PPC 单独界面维护），与需求 26/27 一致 | 需求 11.3、11.4 改写 | 无 | ☐ |
| 4 | 迁移方式未定义 | 新增批量迁移方案：支持 Excel 与结构化文本；逐条校验必填与枚举；失败跳过并记录原因；输出迁移结果报告；状态置新增并保留原命名规则 | **需求 41**（6 条 AC） | 确认源格式与存量数量级 | ☐ |
| 5 | 作废前置校验缺失 | 新增：存在执行中关联 JOB 则阻止作废；已被历史工包引用**允许**作废且历史引用版本记录不变；作废后不可被新工包选用；须填作废原因 | **需求 42**（5 条 AC）<br>Property 24 | 确认「历史工包已引用仍可作废」是否符合业务预期 | ☐ |
| 6 | 工卡类型与商务分类未建映射 | 新增可维护映射表，至少含 01→Gear Inspection、10→SB/AD/SL、11→Material Special Replacement/Configuration(MOD)；一对多时按需求 29.2 判定 | **需求 43**（4 条 AC） | 补充完整映射关系 | ☐ |

---

## 四、贵方待回填项（不计入我方问题，回填后复核）

| 序 | 事项 | 现状 | 影响范围 | 我方承诺 |
|----|------|------|---------|---------|
| 1 | 蓝图 2.2.1 TASK CARD 主数据章节内容错置 | 字段清单误填为「会计科目/科目组/损益科目」等财务内容；系统定义与编码规则均为「无」 | 需求 6（元数据字段）目前系按界面截图反推 | 贵方补充完整字段清单与编码规则后，据此复核需求 6 并出增补对照表 |
| 2 | Ctrl Code 正式值域 | 描述列错位、与 Skill 代码重复 | 需求 6.7、design.md `CTRL_CODE` | 回填后更新值域并移除待确认标记 |
| 3 | 工卡状态表述「两种状态」与三取值不符 | 蓝图原文矛盾 | 需求 4、34 | 我方已按三值扩展为四值（增「审核中」）处理，**请确认此扩展获批** |
| 4 | 工卡类型清单内部矛盾 | 「其他需求」文字清单含「部件卡」「预修理方案工卡」，但 01–11 编码表无此二者，另有 IR卡/AD·SB·SL卡/客户特殊要求卡 | 需求 16.1 | 我方按 01–11 编码表实现，**请裁定是否需增补** |

---

## 五、数据模型结构性影响汇总

以下为本次变更引起的数据模型调整，供设计评审重点复核：

| 类别 | 变更内容 | 触发需求 |
|------|---------|---------|
| 状态枚举 | `CARD_STATUS` 由 3 值扩为 4 值，新增 `UnderReview`；新增 `ALLOWED_TRANSITIONS` 迁移矩阵 | 34 |
| 域分离 | 编制域（`task_card` / `process_step`）与执行域（`job` / `job_process`）**分表存储**，JOB 数据写入不修改来源工卡 | 37 |
| 条码迁移 | 条码列**从 `process_step` 移出**至 `job_process`，约束 `UNIQUE(job_id, process_id)` + `UNIQUE(barcode_value)` | 15 |
| 唯一性 | 工卡唯一性明确为 `(task_no, revision)`；提交查重仅拦截同版本内重复 | 38 |
| 新增表（7） | `job`、`job_process`、`review_record`、`capability_list`、`print_template`、`card_type_commercial_map`、`system_parameter`、`exec_doc_type` | 34/37/39/40/43/35/16 |
| 新增列 | `task_card`: `start_time`、`finish_time`、`job_no`、`commercial_classification_source`<br>`process_step`: `start_time`、`finish_time` | 33/29/37 |
| 正确性属性 | 17 → 24 条；新增 P18 起止时间时序、P19 审核意见归档、P20 复制升版规则、P21 能力校验、P22 打印模板、P23 分类派生、P24 作废校验；P1、P11 重写 | — |

---

## 六、评审确认通过部分（无变更）

以下条目经贵方逐条核对与蓝图一致，本次**未作修改**：需求 1、2、3、5.2、6.3–6.6、7.1–7.3、8、9、10、12.1、13、14、16.2、16.5、18、19、20、21、23、24、26、27、28、30、31、32，及 16.3。

其中贵方特别认可：需求 21（已正确吸收「提升点」自动关联诉求）、需求 31（E-work card 安全警示含确认人与时间留痕，超出预期）。

---

## 七、签核

| 角色 | 姓名 | 意见 | 日期 | 签字 |
|------|------|------|------|------|
| 业务架构师 | | ☐ 同意进入设计阶段　☐ 需再修改 | | |
| TS 业务代表 | | ☐ 同意　☐ 需再修改 | | |
| 项目经理 | | ☐ 同意　☐ 需再修改 | | |

**遗留事项**：第四部分 4 项待贵方回填/裁定，其中第 3、4 项（状态扩展获批、工卡类型是否增补）需在本表签核时一并明确，否则将作为设计阶段风险登记。

---

# 附：签核备忘（第二轮复审回应）

| 项目 | 内容 |
|------|------|
| 复审结论 | 有条件通过，可进入设计阶段（M1、M2 改完即可签核） |
| 本轮变更 | 需求 43 → 44 条；正确性属性 24 → 26 条 |
| 日期 | 2026-08-09 |

## A. 必须修正项（进设计前）— 2 项，均已完成

| 编号 | 评审指出的问题 | 我方处理 | 落地条目 | 状态 |
|------|--------------|---------|---------|------|
| **M1** | 升版后原生效版本状态处置未定义，与需求 38.7 冲突；生效态唯一出路是「生效→作废」，导致双生效或误用作废语义 | **接受并采纳 Superseded 方案**（未选择自动作废方案，因作废语义为「主动停用」且会触发 42.5 原因填报，与版本取代不符）。新增「已被取代(Superseded)」终态，新版本批准生效时**同一事务内**将原生效版本自动迁至该态并写入取代记录；取代失败则回滚该次批准；明确不填作废原因、不走作废流程 | **需求 44**（8 条 AC，新增）<br>Glossary 状态枚举扩至 5 值<br>需求 4.5 迁移矩阵增「生效→已被取代」<br>需求 4.7 新增终态不可迁出<br>需求 2.2、4.4 枚举同步<br>Property 25、26（新增）<br>`ALLOWED_TRANSITIONS` 更新<br>新增表 `supersede_record`<br>任务 20.3b | ✅ 已改 |
| **M2** | 需求 2.2 状态枚举未同步四值，清单展示不到「审核中」，界面上一编一审流程断裂 | 需求 2.2 枚举同步为五值（新增/审核中/生效/已被取代/作废） | 需求 2.2 | ✅ 已改 |

**M1 说明**：确系本轮我方新引入的逻辑缺口。三条需求（38.5 升版置新增、38.7 单一生效、34.4 批准置生效）串联后缺失取代动作，属状态机设计遗漏，非表述问题。已按贵方建议引入独立终态而非复用作废。

## B. 建议澄清项 — 4 项，均已在需求中落地（不留至设计阶段）

| 编号 | 澄清事项 | 我方处理 | 落地条目 |
|------|---------|---------|---------|
| **C1** | 批量复制新编号生成规则缺失，20 张卡逐张人工指定不可用 | 新增按规则自动生成（前缀/后缀/起始序号/步长）、自动跳过占用序号、支持事后逐张调整并复校 | 需求 38.8、38.9、38.10<br>新增表 `task_no_sequence` |
| **C2** | 「提交」一词三义；提交审核校验散落 10.4/10.5、39.2、19.2 三处 | 术语明确为【保存】/【提交审核】/【发布】三分；需求 34.1 集中列出提交审核完整校验清单 (a)–(e)：查重、枚举合法性、必填完整性、能力清单、变更原因；34.2 定义任一不通过的阻断与提示 | 需求 34.1、34.2 改写 |
| **C3** | 需求 26 与 37.5 域归属张力，编制态会出现一排永久空白栏位 | 需求 26.2 改为：编制态（模板视图）**不呈现**执行字段栏位；26.3 新增：仅通过 JOB 实例上下文查看时呈现并带出实际值 | 需求 26.2、26.3 |
| **C4** | 映射表缺 02–09 → Routine；01 双值可能性未纳入判定顺序 | 需求 43.2 补全 01–11 全部映射（02–09 → Routine）；需求 29.2 增加 01 的判定顺序（优先 Gear Inspection，已被工包划为 Routine 时取 Routine）及 02–09 → Routine 规则 | 需求 43.2、29.2 |

## C. 贵方回填与裁定 — 6 项，已全部纳入

| 序 | 贵方裁定 | 我方执行 |
|----|---------|---------|
| 1 | 蓝图 2.2.1 主数据章节内容错置为我方文档缺陷；本周内补充；现行按截图反推做法予以认可 | 记录为遗留事项，待贵方回填后出**增补对照表**复核需求 6 |
| 2 | Ctrl Code 需向 TS 业务确认后回填；**回填前假定 Ctrl Code 与 Skill 为独立值域（同码不同义），不做代码合并** | 需求 6.7 保留待确认标记；**新增需求 6.10** 显式约束二者为独立值域、不合并代码、不共用字典 |
| 3 | **状态扩展为四值 —— 批准**；「两种状态」为文字疏漏，实为三值；并要求连同 M1 的 Superseded 一并纳入 | 已执行，最终为**五值**（新增/审核中/生效/已被取代/作废） |
| 4 | **工卡类型不增补 —— 裁定**：部件卡不单列（已被 04、09 覆盖）；预修理方案工卡不进 01–11（已由需求 16.2 的 PR 覆盖，属执行期衍生单据）；需求 16.1 按 01–11 实现即为准确 | 需求 16.1 维持不变；对照表第四部分第 4 项**结项**，我方不再列为风险 |
| 5 | 需求 42.3「历史工包已引用仍可作废」符合业务预期；但 **42.1 校验范围需扩围** | 需求 42.1 扩至三类引用：(a) 执行中 JOB、(b) 已生成未开始执行的 JOB、(c) 在编（未释放）工包中已选入的引用；42.2 增加提示引用类型与位置 |
| 6 | 迁移源格式存量以 **Word 为主**，Excel 为少量清单类 | 需求 41.2 增加 Word（.doc/.docx）与 RTF，并明确 Word/RTF 为主要迁移通道；量级待贵方下周提供后据此复核迁移方案 |

## D. 本轮数据模型增量

| 类别 | 变更 | 触发 |
|------|------|------|
| 状态枚举 | 4 值 → **5 值**，新增 `Superseded`；`ALLOWED_TRANSITIONS` 增 `Effective→Superseded`；`Superseded`/`Void` 双终态 | M1 / 需求 44 |
| 新增表（2） | `supersede_record`（取代关系）、`task_no_sequence`（批量编号规则） | 需求 44.2 / 38.8 |
| 正确性属性 | 24 → **26** 条；新增 P25 单一生效版本不变式、P26 终态不可迁出与内容冻结；P1、P20 同步改写 | M1 / C1 |
| 任务 | 新增 20.3b（版本取代，独立波次 13）；20.3 扩充批量编号；20.4 扩至 P25/P26 | M1 / C1 |

## E. 覆盖度确认（贵方复审结论摘录）

- 蓝图 2.3.4 全部内容已有需求条目承接：用户需求 I.1–I.11 与 II、其他需求 8 项、工卡类型 11 类、执行单据 11 类、商务分类 9 类、五组下拉值域、三大界面、工作分解步骤 10–90、提升/变革点。
- 模块边界声明清晰（Work Scope / WPL / NRC / JOB 执行域）。
- I.10 适航硬项已由需求 33 闭环至打印输出（33.9 与 5.3 对齐）。
- 上轮「确认通过」26 条本次复查未见回退。
- 贵方特别认可三处抓根因的修正：需求 15（条码约束改 JOB No + Process ID 并禁编制态生成）、需求 29（分类改派生并留存依据来源）、需求 38（唯一性改 (Task No, Revision) 并收窄查重范围）。

## F. 遗留事项（更新）

| 序 | 事项 | 责任方 | 时间 | 状态 |
|----|------|-------|------|------|
| 1 | 蓝图 2.2.1 TASK CARD 主数据字段清单与编码规则回填 | 业务方 | 本周内 | 待回填，回填后出增补对照表复核需求 6 |
| 2 | Ctrl Code 正式值域回填 | 业务方 | 待定 | 待回填；期间按独立值域处理（需求 6.10） |
| 3 | 迁移存量数量级 | 业务方 | 下周 | 待提供，据此复核需求 41 迁移方案 |
| 4 | 各类工卡/单据现行纸质格式样张 | 业务方 | 设计阶段前 | 待提供，支撑需求 40 模板配置 |
| 5 | 状态扩展批准 | 业务方 | — | ✅ 已裁定（批准，含 Superseded） |
| 6 | 工卡类型是否增补 | 业务方 | — | ✅ 已裁定（不增补，结项） |

## G. 签核

| 角色 | 姓名 | 意见 | 日期 | 签字 |
|------|------|------|------|------|
| 业务架构师 | | ☐ M1/M2 已确认修正，同意进入设计阶段　☐ 需再修改 | | |
| TS 业务代表 | | ☐ 同意　☐ 需再修改 | | |
| 项目经理 | | ☐ 同意　☐ 需再修改 | | |

> M1、M2 已于本备忘出具前完成修改，C1–C4 亦已一并落地至需求条目（未留至设计评审）。遗留事项 1–4 为业务方待回填项，不阻塞设计阶段启动。
